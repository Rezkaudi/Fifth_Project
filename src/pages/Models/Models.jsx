import { Outlet } from 'react-router-dom'
import './Models.css'

const Models = () => {
    return (
        <div>
            Models
            <Outlet />
        </div>
    )
}

export default Models